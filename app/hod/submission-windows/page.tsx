import { HodSubmissionWindowsScreen } from "@/features/hod/screens/hod-submission-windows-screen";

export const metadata = {
  title: "Result Submission Windows | HOD Portal",
  description: "Open and manage result submission windows for departmental courses",
};

export default function HodSubmissionWindowsPage() {
  return <HodSubmissionWindowsScreen />;
}
